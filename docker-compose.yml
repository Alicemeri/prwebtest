services:
 base:
  build:
    context: .
    dockerfile: Dockerfile
    no_cache: true
    args:
      USERNAME: # À Compléter
      USERID:   # À Compléter
  container_name: fw1-cc1-test
  command: /bin/bash
  tty: true
  stdin_open: true
  volumes:
  - .:/home/user/workspace
  ports:
  - "8087:8000"
  environment:
    PS1: "[ $$(whoami) | \\w ] "