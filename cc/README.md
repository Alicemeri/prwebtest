# Membres du groupe
# Walid Fettane

# meriem OULD TALEB meriem.ould-taleb@etu.univ-orleans.fr

# aya hassani 

# erwan 



---

# la question 1

 USERNAME=$(id -un) USERID=$(id -u) docker compose up -d

((django) ) [ jax | ~/workspace ] django-admin startproject cc

((django) ) [ jax | ~/workspace ] cd cc

((django) ) [ jax | ~/workspace/cc ] python manage.py startapp medico

((django) ) [ jax | ~/workspace/cc ] python manage.py runserver 0.0.0:8000&


## question2
### dev


 medico/views.py (vue `about`)
 medico/templates/medico/about.html (template)
 medico/urls.py (route `about/`)
 cc/urls.py (inclusion des URLs de l'app)

 python manage.py runserver
# ouvrir http://127.0.0.1:8000/about/

// sur brache aya chez elle elle
git add .
git commit -m "question2 :ajout vue about/
//la elle devrai faire push vers orining aya nrmlm , mais no , elle fera merge vers sa main local puis 
push vers main origin et ajoutera tag question2 puis elle push aussi vers origin main 

git checkout main
git pull origin main
git merge aya -m " question2 par Aya"
git push origin main
git tag question2
git push origin question2


si elle fini ca main aura tag1 et tag2 en place 

